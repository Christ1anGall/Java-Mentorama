/**
 * stramAndCollerctos
 */
public class stramAndCollerctos {

    /*
     * List<Cliente> clientes = Arrays.asList(ele1, ele2, ele3);
     * 
     * Stream<Cliente> stream = clientes.stram().filter(cliente ->
     * cliente.getCompras() > 10);
     * 
     * List<Cliente> selecionados = stream.collect(Collectors.toList());
     * 
     * selecionados.forEach(c -> System.out.println(c.getCompras()));
     */
}